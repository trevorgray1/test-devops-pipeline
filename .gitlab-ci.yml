# GitLab CI/CD pipeline for sample-app

stages:
  - test
  - build
  - publish

test:
  stage: test
  image: python:3.9
  script:
    - python -m pip install --upgrade pip
    - pip install -r sample-app/requirements.txt
    - cd sample-app/testpkg
    - python -m pytest test_testpkg.py
  artifacts:
    paths:
      - sample-app/testpkg/__pycache__/

build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t sample-app:latest ./sample-app
  artifacts:
    paths:
      - sample-app/Dockerfile
  dependencies:
    - test

publish:
  stage: publish
  image: python:3.9
  script:
    - pip install cloudsmith-cli
    - cloudsmith push docker $CLOUDSMITH_OWNER/$CLOUDSMITH_REPO sample-app:latest
  dependencies:
    - build
  only:
    - main
  # Set CLOUDSMITH_OWNER, CLOUDSMITH_REPO, CLOUDSMITH_API_KEY in GitLab CI/CD > Settings > Variables
